// place files you want to import through the `$lib` alias in this folder.

import { basename, sleep } from "./util"
import { toggleWait, wait } from "./config"

export { basename, sleep, wait, toggleWait }